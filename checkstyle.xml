<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">
<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="warning"/>
    <property name="fileExtensions" value="java, properties, xml"/>

    <!-- Allow suppression via @SuppressWarnings("checkstyle:xxx") or comment-style -->
    <module name="SuppressionFilter">
        <property name="file" value="${checkstyle.suppressions.file}" default="checkstyle-suppressions.xml"/>
        <property name="optional" value="true"/>
    </module>

    <!-- Line length limit (must live under Checker, not TreeWalker) -->
    <module name="LineLength">
        <property name="max" value="140"/>
    </module>

    <module name="TreeWalker">
        <!-- Forbid System.out/System.err (DataSourceUrlCustomizer is suppressed) -->
        <module name="RegexpSinglelineJava">
            <property name="format" value="System\.(out|err)\.print"/>
            <property name="ignoreComments" value="true"/>
            <property name="message" value="Avoid System.out/System.err — use SLF4J (@Slf4j) or a logger declared in this class"/>
        </module>
        <!-- Forbid printStackTrace -->
        <module name="RegexpSinglelineJava">
            <property name="format" value="\bprintStackTrace\s*\("/>
            <property name="ignoreComments" value="true"/>
            <property name="message" value="Avoid printStackTrace — use log.error(&quot;...&quot;, e) to attach the stack trace"/>
        </module>
        <!-- JavaDoc on public/protected (JavadocMethod in checkstyle 9.3 uses accessModifiers instead of scope) -->
        <module name="JavadocMethod">
            <property name="accessModifiers" value="public"/>
        </module>
    </module>
</module>
